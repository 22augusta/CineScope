{
  "version": 2,
  "builds": [
    {
      "src": "MovieExplorer/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "MovieExplorer/dist"
      }
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
